pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
        // Add Flutter SDK as a plugin source
     
    }
    includeBuild("${System.getenv("FLUTTER_HOME") ?: "../flutter"}")
}

include(":app")
